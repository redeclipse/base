shader 0 compositehint [
    attribute vec4 vvertex, vcolor;
    attribute vec2 vtexcoord0;
    uniform mat4 hudmatrix;
    varying vec2 texcoord0;
    varying vec4 colorscale;

    void main(void)
    {
        gl_Position = hudmatrix * vvertex;
        texcoord0 = vtexcoord0;
        colorscale = vcolor;
    }
] [
    uniform vec3 size; // xradius yradius blendstart
    varying vec2 texcoord0;
    varying vec4 colorscale;

    fragdata(0) vec4 fragcolor;

    void main()
    {
        float dist = length((texcoord0 - vec2(0.5)) * size.xy) * 2.0;
        float blend = 1.0 - smoothstep(size.z, 1.0, dist);
        fragcolor = vec4(colorscale.rgb, colorscale.a * blend);
    }
]

newcompui hint [
    @(setlocal size [1.0 1.0 0.0])
    execid uicompargs
    uicomp compositehint 1 1 [
        uicompparam size $size1 $size2 $size3
    ]
]

newcompui hintbold [
    @(setlocal size [1.0 1.0 0.65])
    execid uicompargs
    uicomp compositehint 1 1 [
        uicompparam size $size1 $size2 $size3
    ]
]

newcompui hintent [
    @(setlocal size [1.0 1.0 0.35])
    execid uicompargs
    uicomp compositehint 1 1 [
        uicompparam size $size1 $size2 $size3
    ]
]

newcompui hintvert [
    @(setlocal size [0.5 1.0 0.0])
    execid uicompargs
    uicomp compositehint 1 1 [
        uicompparam size $size1 $size2 $size3
    ]
]

newcompui hinthorz [
    @(setlocal size [1.0 0.5 0.0])
    execid uicompargs
    uicomp compositehint 1 1 [
        uicompparam size $size1 $size2 $size3
    ]
]

newcompui stain [
    @(setlocal size [1.0 0.5 0.0])
    execid uicompargs
    uicomp compositehint 1 1 [
        uicompparam size $size1 $size2 $size3
        uisetcolour 0x808080
    ]
]
