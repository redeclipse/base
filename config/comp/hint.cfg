shader 0 compositehint [
    attribute vec4 vvertex, vcolor;
    attribute vec2 vtexcoord0;
    uniform mat4 hudmatrix;
    varying vec2 texcoord0;
    varying vec4 colorscale;

    void main(void)
    {
        gl_Position = hudmatrix * vvertex;
        texcoord0 = vtexcoord0;
        colorscale = vcolor;
    }
] [
    uniform vec3 params;
    varying vec2 texcoord0;
    varying vec4 colorscale;

    fragdata(0) vec4 fragcolor;

    void main()
    {
        float dist = length((texcoord0 - vec2(0.5)) * params.xy) * 2.0;
        float blend = 1.0 - smoothstep(params.z, 1.0, dist);
        fragcolor = vec4(colorscale.rgb, colorscale.a * blend);
    }
]

newcompui hint [
    @(setlocal params [1.0 1.0 0.0])
    execid uicompargs
    uialign -1 -1
    uicomp compositehint 1 1 [
        uicompparam params $params1 $params2 $params3
    ]
]

newcompui hintbold [
    @(setlocal params [1.0 1.0 0.65])
    execid uicompargs
    uialign -1 -1
    uicomp compositehint 1 1 [
        uicompparam params $params1 $params2 $params3
    ]
]

newcompui hintent [
    @(setlocal params [1.0 1.0 0.35])
    execid uicompargs
    uialign -1 -1
    uicomp compositehint 1 1 [
        uicompparam params $params1 $params2 $params3
    ]
]

newcompui hintvert [
    @(setlocal params [0.5 1.0 0.0])
    execid uicompargs
    uialign -1 -1
    uicomp compositehint 1 1 [
        uicompparam params $params1 $params2 $params3
    ]
]

newcompui hinthorz [
    @(setlocal params [1.0 0.5 0.0])
    execid uicompargs
    uialign -1 -1
    uicomp compositehint 1 1 [
        uicompparam params $params1 $params2 $params3
    ]
]

newcompui stain [
    @(setlocal params [1.0 0.5 0.0])
    execid uicompargs
    uialign -1 -1
    uicomp compositehint 1 1 [
        uicompparam params $params1 $params2 $params3
        uisetcolour 0x808080
    ]
]
