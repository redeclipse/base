tool_texinfo_props = [
    [ uit_disabled 0 ]
    [ uit_text_size $ui_tool_small_text_size_unscaled ]
    [ uit_decal 0 ]
]

// 1:<slot> 2:<props>
ui_tool_texinfo = [
    @(tool_props $tool_texinfo_props arg2)

    @(tool_ui_scale uit_text_size)

    local _namefunc
    _namefunc = (? $uit_decal getdecalname gettexname)

    if $uit_disabled [
        uitext "-" $uit_text_size
    ] [
        if (> $arg1 -1) [
            uitext (format "#%1: %2" $arg1 ($_namefunc $arg1)) $uit_text_size
        ] [
            uitext "No texture selected" $uit_text_size
        ]
    ]
]
