sound_press  = (registersound "sounds/interface/press"  255) // S_PRESS (something pressed)
sound_back   = (registersound "sounds/interface/back"   255) // S_BACK (something dismissed)
sound_change = (registersound "sounds/interface/change" 255) // S_ACTION (something happened)
