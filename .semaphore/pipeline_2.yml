version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Execute
    task:
      jobs:
        - name: Script
          commands:
            - checkout
            - 'curl -H "Cache-Control: no-cache" --fail --output "${HOME}/deploy.sh" "https://raw.githubusercontent.com/redeclipse/base/${SEMAPHORE_GIT_BRANCH}/.semaphore/deploy.sh"'
            - 'chmod +x "${HOME}/deploy.sh"'
            - 'cat "${HOME}/deploy.sh"'
            - '. "${HOME}/deploy.sh" steam'
